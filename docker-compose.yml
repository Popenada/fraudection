services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fraudection
    ports:
      - "5432:5432"
    volumes:
      - fraudection-pg-data:/var/lib/postgresql/data

volumes:
  fraudection-pg-data:
